<div class="container-fluid" data-ng-controller="DeputadoFederalSecretariosDetalhesController">
	<fieldset>
		<legend>Secretários Parlamentares de {{::NomeParlamentar}}</legend>

		<div class="table-responsive">
			<table class="table table-striped table-hover">
				<thead>
					<tr>
						<th>#</th>
						<th>Nome do Secretário</th>
					</tr>
				</thead>
				<tr ng-repeat="row in deputado_federal_secretarios">
					<td>{{::row.$id}}</td>
					<td style="width:90%" sortable="'NomeSecretario'">{{::row.NomeSecretario}}</td>
				</tr>
			</table>
		</div>
	</fieldset>
</div>

